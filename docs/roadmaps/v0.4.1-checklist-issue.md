- [ ] Keep public APIs stable across `rave-*` crates (no API churn unless bugfix/regression fix)
- [ ] Verify all Linux CI jobs are green after OpenSSL/pkg-config/FFmpeg env fixes
- [ ] Reduce clippy exception surface by fixing easy code warnings
- [ ] (Optional) Refactor repeated CI shell env cleanup (`unset FFMPEG_DIR LIBCLANG_PATH`)
- [ ] Fix `rave-pipeline` dead-code warning in `GraphBackend` (`ctx`, `kernels` fields)
- [ ] Fix irrefutable `let ... else` in `crates/rave-pipeline/src/stage_graph.rs` tests
- [ ] Replace MSRV-incompatible `is_multiple_of` usage in `rave-core` (`host_copy_audit.rs`) with Rust `1.85`-compatible check
- [ ] Add “Which crate do I use?” section in root `README.md`
- [ ] Add minimal integration example (`rave-pipeline` + `rave-runtime-nvidia`)
- [ ] Add maintainer release doc (`docs/releasing.md`) covering publish order, `--no-verify` guidance, and tag retarget flow
- [ ] Decide whether WSL-specific rustflags remain in shared `.cargo/config.toml`
- [ ] If WSL rustflags move out of repo config, document local-only Windows/WSL FFmpeg/libclang/pkg-config overrides
- [ ] Review crates.io/docs.rs feedback/issues for `0.4.0`
- [ ] Collect install/build failures from users (Linux/Windows)
- [ ] Clarify any confusion around `rave-pipeline` vs `rave-runtime-nvidia`
- [ ] Prepare `CHANGELOG.md` entry for `0.4.1`
- [ ] Run package/docsrs/publish dry-run checks
- [ ] Publish patch versions in dependency order if any crate code changes require release
