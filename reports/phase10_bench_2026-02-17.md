# Phase 10 Benchmark Verification Report (Blocked)

## 1. Baseline Reference

- Baseline source: **MISSING**
  - No prior baseline report found under `reports/`.
  - No last-good benchmark commit SHA/tag reference found in repo docs/notes.
- Tolerances used: **repo-defined tolerances** from `docs/rave_Phase10_Execution_Contract.md`
  - FPS regression <= 1%
  - VRAM delta <= 1%
  - Pool hit rate >= 95%
  - Identical SHA256 hashes

## 2. Environment

- OS: Ubuntu 24.04.4 LTS (`/etc/os-release`)
- GPU: NVIDIA GeForce RTX 3080 Ti (`nvidia-smi -L`)
- NVIDIA driver: 591.86 (`nvidia-smi --query-gpu=name,driver_version --format=csv,noheader`)
- CUDA (driver-reported): 13.1 (`nvidia-smi --version`)
- TensorRT: not detected in this environment (`trtexec` not found; no `dpkg` TensorRT package entries)
- Rust toolchain:
  - `cargo --version`: **FAILED** (`cargo: command not found`)
  - `rustc --version`: not available (same toolchain/PATH issue)

## 3. Entrypoint Selected

- Selected entrypoint (planned): `rave-cli` pipeline run (`cargo run -p rave-cli -- ...`)
- Why:
  - `examples/benchmark.rs` exists but is a no-op stub (does not run pipeline or emit benchmark metrics).
  - No Criterion or `cargo bench` benchmark targets found.
  - CLI path is the only existing route that emits runtime logs (frame counters, stage latencies, pool stats, VRAM).
- Alternatives found:
  - `examples/benchmark.rs` (stub)
  - `cargo bench` targets: none

## 4. Commands Executed

```bash
pwd && ls -la
rg --files reports
rg -n "phase10_bench_baseline|baseline|tolerance|threshold|last-good|last good" reports docs README* **/*.{md,txt}
ls -la docs && ls -la examples && ls -la rave-cli
rg -n "benchmark|bench|metrics|BucketedPool|hit rate|queue|latency|determin|sha256|tolerance|threshold|phase10" docs README.md ARCHITECTURE.md examples src crates rave-cli -g '*.md' -g '*.rs'
sed -n '1,220p' examples/benchmark.rs
sed -n '1,560p' rave-cli/src/main.rs
sed -n '1,260p' README.md
git tag --list
rg -n "last-good|last good|baseline.*(sha|commit|tag)|phase10_bench_baseline" docs README.md ARCHITECTURE.md -g '*.md'
rg --files | rg -n '\.onnx$|model'
cat /etc/os-release
cargo --version && rustc --version
nvidia-smi --query-gpu=name,driver_version --format=csv,noheader && nvidia-smi --version
which cargo
ls -la ~/.cargo/bin
ls -la /mnt/c/Users/Calvin/.cargo/bin
nvidia-smi -L
/mnt/c/Users/Calvin/.cargo/bin/cargo.exe --version && /mnt/c/Users/Calvin/.cargo/bin/rustc.exe --version
ls -la engine-v2/test_videos && ls -la engine-v2/test_videos/_bench_out
nvcc --version
trtexec --version
dpkg -l | rg -i "tensorrt|libnvinfer"
rg -n "criterion|\[bench\]|cargo bench|benchmark" Cargo.toml crates/**/Cargo.toml rave-cli/Cargo.toml examples/*.rs
sed -n '1,220p' Cargo.toml
```

Required but blocked commands:

```bash
cargo build --workspace
cargo test --workspace
```

## 5. Warm-up Notes

- Warm-up run: **NOT EXECUTED** (blocked before benchmark execution).
- First-run build/cache detection (TRT engine build/JIT): **NOT OBSERVED** (no runnable benchmark execution occurred).

## 6. Results (3 Trials)

All trials blocked before execution due missing runnable Rust toolchain in current shell environment.

| Trial | Wall Time | FPS | Peak VRAM | Pool Hit Rate | Exit Code |
|---|---|---|---|---|---|
| Warm-up | BLOCKED | BLOCKED | BLOCKED | BLOCKED | N/A |
| Trial 1 | BLOCKED | BLOCKED | BLOCKED | BLOCKED | N/A |
| Trial 2 | BLOCKED | BLOCKED | BLOCKED | BLOCKED | N/A |
| Trial 3 | BLOCKED | BLOCKED | BLOCKED | BLOCKED | N/A |

Baseline deltas: **N/A** (baseline missing and no trial data).

## 7. Queue Depths + Per-Stage Latency

- Queue depths: **MISSING**
  - Reason: no benchmark run logs were produced.
- Per-stage latency: **MISSING**
  - Reason: no benchmark run logs were produced.

## 8. Determinism

- Output artifacts hashed: **NONE**
- SHA256 values: **N/A**
- Determinism status: **NOT EVALUATED** (no benchmark output file produced)

## 9. Verdict

- **NO VERDICT: execution blocked before benchmark trials**
  - Baseline is missing.
  - Required build/test/benchmark commands could not be run in this shell due unavailable Cargo toolchain.

## 10. Failures / Blockers

- Blocker 1: Rust toolchain unavailable on PATH
  - Error:
    - `/bin/bash: line 1: cargo: command not found`
  - Minimal repro:
    1. `cd /mnt/c/Users/Calvin/Software Projects/rave`
    2. `cargo --version`

- Blocker 2: Windows Cargo binary not executable from this WSL session
  - Error:
    - `<3>WSL (1514 - ) ERROR: UtilBindVsockAnyPort:307: socket failed 1`
  - Minimal repro:
    1. `cd /mnt/c/Users/Calvin/Software Projects/rave`
    2. `/mnt/c/Users/Calvin/.cargo/bin/cargo.exe --version`

- Input file presence check:
  - `engine-v2/test_videos/Input.mp4` exists.

- STOP: no fixes applied, no code changes made.
